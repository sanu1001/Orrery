# Alternative to GitHub Pages. Netlify has no Go toolchain by default, so the
# traces are generated in the build command via the Go build image.
[build]
  command = "make traces && cd web && npm ci && npm run build"
  publish = "web/dist"

[build.environment]
  GO_VERSION = "1.24"
  NODE_VERSION = "22"
