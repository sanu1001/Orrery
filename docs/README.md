# docs/

The curated public subset. Populated from the private workshop at ship time —
see `ADR/0018-repo-layout.md` for what gets promoted and why.

| File | Source | Trim |
|---|---|---|
| `TRACE_FORMAT.md` | workshop | verbatim |
| `ARCHITECTURE.md` | workshop | verbatim, minus workshop-only cross-references |
| `adr/` | workshop `ADR/` | verbatim, minus 0017 (naming) and 0018 (this split) |
| `img/` | new | empty — screenshots pending re-capture (ROADMAP A16) |

Everything else stays private. `FLAWS.md` in particular is written adversarially
for personal use: knowing its contents in an interview reads extremely well,
publishing it does not.
